= simple_form_for([:my, @ground], :html => {:class => 'form-horizontal' }) do |f|
	.control-group= f.input :url, :input_html => { :class => "span6" }
	= f.button :submit, :class => 'btn-primary'
	= link_to 'Cancel', my_grounds_path, :class => "btn btn-danger"