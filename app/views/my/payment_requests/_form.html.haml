= simple_form_for(@payment_request, :html => {:class => 'form-horizontal' }) do |f|
	= f.input :wallet, :input_html => { :class => "span6" }
	= f.button :submit, 'Запросить выплату', :class => 'btn-primary'
	= link_to 'Отмена', root_path, :class => "btn btn-danger"